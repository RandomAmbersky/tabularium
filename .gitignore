target
# Ephemeral staging for `just pub-docker` (per-arch binaries for Dockerfile.publish).
docker/_build/
make-deb/dist/
.DS_Store
# Local server config (copy from config.toml.example).
config.toml
/data/
.venv/
*.sqlite
*.sqlite-wal
*.sqlite-shm
__pycache__
exported
.brew-dist/
